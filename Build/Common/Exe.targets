<Project>
  <!--
    実行ファイル関連の設定
    このファイルは、Directory.Build.targetsでインポートする。
  -->

  <Target Name="_CheckExeSettings" BeforeTargets="Publish">
    <Error Condition="'$(PublishMode)' == ''" Text="PublishMode is required." />
  </Target>

</Project>
