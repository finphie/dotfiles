name: Build(.NET)

on:
  pull_request:
    paths:
      - '**/*.cs'
      - '**/*.tt'
      - '**/*.xaml'
      - '**/*.razor'
      - '**/*.cshtml'
      - '**/*.csproj'
      - '**/*.sln'
      - '**/*.props'
      - '**/*.targets'
      - '**/.editorconfig'
      - '**/.globalconfig'
      - .github/workflows/build-dotnet.yml

permissions: {}

jobs:
  main:
    uses: finphie/Actions/.github/workflows/build-dotnet.yml@main
